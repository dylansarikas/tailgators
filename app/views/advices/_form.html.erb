<%= form_with(model: advice) do |form| %>
  <% if advice.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(advice.errors.count, "error") %> prohibited this advice from being saved:</h2>

      <ul>
        <% advice.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.label :adviceable_id, style: "display: block" %>
    <%= form.text_field :adviceable_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
