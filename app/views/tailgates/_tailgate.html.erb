<div id="<%= dom_id tailgate %>">
  <div class="card" style="width: 18rem;">
    <div class="card-body">
      <h5 class="card-title"><%= tailgate.name %></h5>
      <h6 class="card-subtitle mb-2 text-body-secondary"><%= tailgate.location %></h6>
      <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
      <%= link_to "#{tailgate.stadium.name}", stadium_path(tailgate.stadium.name), class: "btn btn-outline-primary m-2" %>
    </div>
  </div>
</div>
